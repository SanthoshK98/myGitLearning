Git Command

git config  //to assign settings like configuration

git config --global user.name "Santhosh"
git config --global user.email "san@gmail.com"

git branch

git merge

git checkout

git checkout -b branch_name

Timestamps: 7:53

git remote add origin https://github.com/SanthoshK98/Git-learning.git
git branch -M main
git push -u origin main

git push --set-upstream origin master